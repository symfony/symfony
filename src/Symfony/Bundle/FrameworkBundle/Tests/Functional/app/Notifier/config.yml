imports:
    - { resource: ../config/default.yml }
    - { resource: services.yml }

framework:
    mailer:
        dsn: 'null://null'
    notifier:
        chatter_transports:
            slack: 'null://null'
            microsoftteams: 'null://null'
        channel_policy:
            urgent: ['chat/microsoftteams']
        admin_recipients:
            - { email: admin@example.com }
    profiler: ~
