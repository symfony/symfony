imports:
    - { resource: ./config.yml }
    - { resource: ./config_session.yml }

framework:
    http_method_override: false
    session:
        cookie_secure: auto
        cookie_samesite: lax

security:
    erase_credentials: false
    firewalls:
        default:
            stateless: true
