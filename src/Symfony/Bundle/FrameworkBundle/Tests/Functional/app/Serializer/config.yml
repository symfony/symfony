imports:
    - { resource: ../config/default.yml }

framework:
    serializer: { enabled: true }
    property_info: { enabled: true }

services:
    serializer.alias:
        alias: serializer
        public: true
