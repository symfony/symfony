imports:
    - { resource: merge_import.yml }

security:
    erase_credentials: false
    providers:
        default: { id: foo }

    firewalls:
        main:
            form_login: false
            http_basic: ~

    role_hierarchy:
        FOO: [MOO]
