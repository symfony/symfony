imports:
    - { resource: ./base_config.yml }

security:
    erase_credentials: false
    firewalls:
        default:
            form_login:
                enable_csrf: true
                csrf_parameter: "user_login[_token]"
