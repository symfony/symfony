framework:
    http_method_override: false
    secret: s3cr3t
    validation:
        enabled:     true
        enable_annotations: true

services:
    validator.alias:
        alias: validator
        public: true
