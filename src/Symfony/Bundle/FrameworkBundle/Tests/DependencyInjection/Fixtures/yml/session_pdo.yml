app.config:
    router:
        resource: %kernel.root_dir%/config/routing.xml
    session:
        storage_id:      pdo
        pdo.db_table:    table
        pdo.db_id_col:   id
        pdo.db_data_col: data
        pdo.db_time_col: time
    templating:
        engine: php
