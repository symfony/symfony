---------------------------------------------------------------------------

by carsonbot at 2026-08-11T06:17:41Z

Hey!

Thanks for your PR. You are targeting branch "8.2" but it seems your PR description refers to branch "8.1".
Could you update the PR description or change target branch? This helps core maintainers a lot.

Cheers!

Carsonbot

---------------------------------------------------------------------------

by nicolas-grekas at 2026-08-11T12:53:14Z

Folded the two adjacent pre-existing bugs in here rather than opening a separate PR, since your fix depends on the first one: applying the align-items offset to descendants goes through the same shift path that was inert from frame 2 onwards.

Reworked `layoutHorizontal()` to mirror what #65268 did to the vertical path, instead of clearing the render cache of every parent child on every frame. Measured with a `ContainerWidget` subclass counting `all()`, an unchanged pane in a horizontal row over 4 frames: 6 calls with the unconditional clear, 3 now. The test comment about the left pane staying cached is true again, and asserted with a counting leaf.

Also quoted the line preview in the `RenderException` message, in its own commit. That is the fabbot placeholder check, which fires on `Renderer.php` now that this PR touches it; its own suggested patch does not compile inside that double-quoted string, whereas the escaped form does and matches the widget class already quoted in the same message.

All four assertions fail on plain 8.1 for the right reasons. Tui suite 1693 tests green.
