imports:
    - { resource: ../config/default.yml }

framework:
    http_method_override: false
    profiler:
        enabled: true
        collect: true

services:
    public_profiler:
        alias: profiler
        public: true
