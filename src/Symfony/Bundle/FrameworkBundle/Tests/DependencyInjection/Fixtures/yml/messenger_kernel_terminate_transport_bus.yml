framework:
    messenger:
        serializer: false
        buses:
            a_bus: ~
            another_bus: ~
        transports:
            kernel_terminate:
                dsn: 'symfony://kernel.terminate'
                options:
                    bus: a_bus
