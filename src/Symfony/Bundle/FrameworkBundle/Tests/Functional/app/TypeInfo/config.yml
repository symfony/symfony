imports:
    - { resource: ../config/default.yml }

framework:
    type_info:
        aliases:
            CustomAlias: int

services:
    type_info.resolver.alias:
        alias: type_info.resolver
        public: true
