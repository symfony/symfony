imports:
    - { resource: ./config.yml }

security:
    erase_credentials: false
    firewalls:
        secure:
            pattern: ^/
            form_login:
                check_path: /login_check
