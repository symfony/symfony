imports:
    - { resource: ./localized_routes.yml }

security:
    erase_credentials: false
    firewalls:
        default:
            form_login:
                use_forward: true
                failure_forward: true
