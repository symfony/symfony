blog_show:
    resource:         validpattern.yml
    prefix:           /{foo}
    defaults:         { 'foo': '123' }
    requirements:     { 'foo': '\d+' }
    options:          { 'foo': 'bar' }
    hostname_pattern: "{locale}.example.com"
