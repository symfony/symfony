imports:
    - { resource: ./../config/framework.yml }

framework:
    http_client: ~

security:
    providers:
        in_memory:
            memory:
                users:
                    john: { password: test, roles: [ROLE_USER] }

    firewalls:
        oidc:
            pattern: ^/oidc
            provider: in_memory
            oidc_login:
                provider_uri: 'https://accounts.example.com'
                client_id: client_id
                client_authentication:
                    client_secret_post: client_secret
        main:
            http_basic: ~
            stateless: true
