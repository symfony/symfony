imports:
    - { resource: ../config/default.yml }
    - { resource: services.yml }

framework:
    mailer:
        dsn: 'smtp://null'
        envelope:
            sender: sender@example.org
            recipients:
                - redirected@example.org
