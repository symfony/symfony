imports:
    - { resource: ../config/default.yml }

framework:
    secret: '%env(SECRETS_TEST_APP_SECRET)%'
    secrets:
        vault_directory: '%kernel.project_dir%/%kernel.test_case%/secrets/%kernel.runtime_environment%'
        decryption_env_var: 'base64:default::SECRETS_TEST_DECRYPTION_SECRET'

parameters:
    secrets_test.foo: '%env(default::SECRETS_TEST_FOO)%'
