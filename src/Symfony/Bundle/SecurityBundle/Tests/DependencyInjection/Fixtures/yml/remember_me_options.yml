security:
    providers:
        default:
            id: foo

    firewalls:
        main:
            form_login: true
            remember_me:
                key: TheKey
                catch_exceptions: false
                token_provider: token_provider_id
