---------------------------------------------------------------------------

by ogizanagi at 2020-04-17T13:59:05Z

@nicolas-grekas : I've updated the PR, so it should work now using `.env` files or `phpunit.xml`
