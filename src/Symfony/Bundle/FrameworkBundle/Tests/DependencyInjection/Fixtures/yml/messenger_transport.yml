framework:
    serializer: true
    messenger:
        serializer:
            format: csv
            context:
                enable_max_depth: true
        transports:
            default: 'amqp://localhost/%2f/messages'
