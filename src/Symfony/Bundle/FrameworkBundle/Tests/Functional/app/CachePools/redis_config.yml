imports:
    - { resource: ../config/default.yml }

framework:
    cache:
        app: cache.adapter.redis
        pools:
            cache.pool1:
                public: true
            cache.pool2:
                public: true
                clearer: ~
