security:
    firewalls:
        with_string:
            access_token:
                token_handler:
                    oauth2: true
                default_roles: 'ROLE_FOO'

        with_array:
            access_token:
                token_handler:
                    oauth2: true
                default_roles: ['ROLE_FOO', 'ROLE_BAR']
