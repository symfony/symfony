# Indonesian validator translations

Indonesian validator messages belong in this directory using the
`validators.id.xlf` filename and the same message identifiers as the English
catalog. Keep placeholders such as `{{ limit }}` unchanged and run the
translation resource tests after adding or updating a catalog.
