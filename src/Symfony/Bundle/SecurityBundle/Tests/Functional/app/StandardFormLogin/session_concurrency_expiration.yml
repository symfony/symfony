imports:
    - { resource: ./session_concurrency.yml }

security:
    firewalls:
        default:
            form_login:
                check_path: /login_check
                default_target_path: /profile
            anonymous: ~
            session_concurrency:
                max_sessions: 1
                error_if_maximum_exceeded: false
                expiration_url: /expired
